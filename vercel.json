{
  "buildCommand": "mkdir -p public/assets && cp -r data/assets/* public/assets/ && mkdir -p public/raw",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  }
}
